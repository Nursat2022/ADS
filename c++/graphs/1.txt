список смежности

второй способ представление графа в памяти используется чаще

список смежности(только присутсвие ребра)

vector<vector<int> > g(n) or vector<int> g[n];
g[0] не используется

g[i] вектор в своем составе имеет номера тех вершин с которыми у меня есть ребро из i-вой вершины
т.е. (2c)


DFS - depth first serch
bullet journal

BFS - breadth first search
изначально у нас точно также выбирается стартовая точка(s)

очень часто bfs сравнивают с поджиганием графа

массив d - растояние